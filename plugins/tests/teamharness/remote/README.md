# Remote TeamHarness Tests

Tests in this directory cover remote-managed local worker packages under
`plugins/teamharness/remote/`.

Runtime-neutral TeamHarness tests stay under `plugins/tests/teamharness/`.
