# Changelog (Unreleased)

Record image-affecting changes to `manager/`, `worker/`, `copaw/`, `hermes/`, `openclaw-base/`, and `agentteams-controller/` here before the next release.

---

- fix(manager): make attached Worker Skill installation safe, deterministic, and independent of `assign_when` for explicit assignments ([pending](https://github.com/agentscope-ai/AgentTeams/commit/pending))
