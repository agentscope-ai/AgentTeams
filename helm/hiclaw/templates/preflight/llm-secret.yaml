{{- if .Values.preflight.llm.enabled }}
{{- $name := printf "%s-llm-preflight" (include "hiclaw.fullname" .) | trunc 63 | trimSuffix "-" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
  namespace: {{ include "hiclaw.namespace" . }}
  labels:
    {{- include "hiclaw.component.labels" (dict "root" . "component" "llm-preflight") | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-failed,hook-succeeded
type: Opaque
stringData:
  HICLAW_LLM_API_KEY: {{ required "credentials.llmApiKey is required" .Values.credentials.llmApiKey | quote }}
  HICLAW_LLM_PROVIDER: {{ .Values.credentials.llmProvider | quote }}
  HICLAW_DEFAULT_MODEL: {{ .Values.credentials.defaultModel | quote }}
  {{- if .Values.credentials.llmBaseUrl }}
  HICLAW_OPENAI_BASE_URL: {{ .Values.credentials.llmBaseUrl | quote }}
  {{- end }}
  HICLAW_LLM_PREFLIGHT_STRICT: {{ .Values.preflight.llm.strict | quote }}
  HICLAW_LLM_PREFLIGHT_TIMEOUT_SECONDS: {{ .Values.preflight.llm.timeoutSeconds | quote }}
  HICLAW_LLM_PREFLIGHT_RETRIES: {{ .Values.preflight.llm.retries | quote }}
{{- end }}
